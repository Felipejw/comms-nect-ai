# ============================================
# CONFIGURAÇÃO DO SISTEMA DE ATENDIMENTO
# Copie este arquivo para .env e preencha os valores
# Suporte a Multi-Instância WPPConnect
# ============================================

# ==========================================
# CONFIGURAÇÕES DO SERVIDOR
# ==========================================

# Domínio ou IP público do servidor (sem http/https)
DOMAIN=seu-dominio.com.br

# Email para certificado SSL (Let's Encrypt)
SSL_EMAIL=seu-email@exemplo.com

# URL externa da API (com protocolo)
API_EXTERNAL_URL=https://seu-dominio.com.br

# URL do site/frontend
SITE_URL=https://seu-dominio.com.br

# URLs adicionais permitidas para redirect (separadas por vírgula)
ADDITIONAL_REDIRECT_URLS=

# ==========================================
# BANCO DE DADOS
# ==========================================

# Senha do PostgreSQL (MUDE ISSO! Use uma senha forte)
POSTGRES_PASSWORD=SuaSenhaSeguraAqui123!

# Nome do banco de dados
POSTGRES_DB=postgres

# Porta do PostgreSQL (padrão: 5432)
POSTGRES_PORT=5432

# ==========================================
# CHAVES JWT (Geradas automaticamente pelo install.sh)
# ==========================================

# Chave secreta JWT (32+ caracteres)
JWT_SECRET=

# Chave anônima (gerada a partir do JWT_SECRET)
ANON_KEY=

# Chave de serviço (gerada a partir do JWT_SECRET)
SERVICE_ROLE_KEY=

# Tempo de expiração do token (em segundos, padrão: 3600 = 1 hora)
JWT_EXPIRY=3600

# ==========================================
# PORTAS
# ==========================================

# Porta HTTP (padrão: 80)
HTTP_PORT=80

# Porta HTTPS (padrão: 443)
HTTPS_PORT=443

# Porta do Kong (padrão: 8000)
KONG_HTTP_PORT=8000

# Porta do Kong HTTPS (padrão: 8443)
KONG_HTTPS_PORT=8443

# ==========================================
# AUTENTICAÇÃO
# ==========================================

# Desabilitar signup público (true/false)
DISABLE_SIGNUP=false

# Confirmar email automaticamente (true = sem verificação de email)
ENABLE_EMAIL_AUTOCONFIRM=true

# Habilitar signup por email
ENABLE_EMAIL_SIGNUP=true

# Habilitar usuários anônimos
ENABLE_ANONYMOUS_USERS=false

# Habilitar signup por telefone
ENABLE_PHONE_SIGNUP=false

# ==========================================
# EMAIL / SMTP (Opcional)
# ==========================================

# Servidor SMTP
SMTP_HOST=

# Porta SMTP (padrão: 587)
SMTP_PORT=587

# Usuário SMTP
SMTP_USER=

# Senha SMTP
SMTP_PASS=

# Email do administrador
SMTP_ADMIN_EMAIL=

# ==========================================
# WPPCONNECT - CONFIGURAÇÃO MULTI-INSTÂNCIA
# ==========================================

# Chave secreta do WPPConnect (GERE UMA CHAVE SEGURA!)
# IMPORTANTE: Use a mesma chave em todas as instâncias
WPPCONNECT_SECRET_KEY=SuaChaveWPPConnectAqui

# --- Instância Primária (sempre ativa) ---
WPPCONNECT_PORT_1=21465
WPPCONNECT_API_URL=http://wppconnect-1:21465

# --- Instância Secundária (opcional) ---
# Ative com: docker-compose --profile multi-instance up -d
WPPCONNECT_PORT_2=21466
WPPCONNECT_API_URL_2=http://wppconnect-2:21465

# --- Instância Terciária (opcional) ---
# Ative com: docker-compose --profile high-availability up -d
WPPCONNECT_PORT_3=21467
WPPCONNECT_API_URL_3=http://wppconnect-3:21465

# --- Load Balancer (opcional) ---
# Ativado automaticamente com perfil multi-instance
WPPCONNECT_LB_PORT=21400
WPPCONNECT_LB_URL=http://wppconnect-lb:80

# --- URL Pública (para acesso externo) ---
WPPCONNECT_SERVER_URL=https://seu-dominio.com.br:21465

# --- Webhook ---
# URL do Webhook para receber mensagens do WhatsApp
WEBHOOK_URL=https://seu-dominio.com.br/functions/v1/wppconnect-webhook

# ==========================================
# GOOGLE CALENDAR (Opcional)
# ==========================================

# Client ID do Google OAuth
GOOGLE_CLIENT_ID=

# Client Secret do Google OAuth
GOOGLE_CLIENT_SECRET=

# ==========================================
# SUPABASE STUDIO (Opcional)
# ==========================================

# Nome da organização
STUDIO_DEFAULT_ORGANIZATION=Minha Empresa

# Nome do projeto
STUDIO_DEFAULT_PROJECT=Sistema de Atendimento

# ==========================================
# VARIÁVEIS DO FRONTEND
# ==========================================

# URL do Supabase (para o frontend)
VITE_SUPABASE_URL=https://seu-dominio.com.br

# Chave pública do Supabase (mesma que ANON_KEY)
VITE_SUPABASE_PUBLISHABLE_KEY=

# ID do projeto (pode deixar em branco)
VITE_SUPABASE_PROJECT_ID=self-hosted
